/* Import Normalize CSS */
@import url('./normalize.css');
/* General Reset */
/* Basic Reset */
* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

body {
    font-family: Arial, sans-serif;
    line-height: 1.6;
    color: #333;
    background-color: #f9f9f9;
    margin: 0;
    padding: 0;
}

/* Header Section */
header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px;
    background-color: #333;
    color: #fff;
    position: relative;
}

header .logo {
    display: flex;
    align-items: center;
}

header .logo img {
    width: 50px;
    height: 50px;
    margin-right: 10px;
}
header .logo span {
    font-size: 1.2em;
    font-weight: bold;
}
/* Circle Styling and Position Adjustment */
.header-circle {
    width: 40px;
    height: 40px;
    background-color: #4CAF50; /* Customize color if needed */
    border-radius: 50%;
    display: flex;
    justify-content: center;
    align-items: center;
    font-size: 1.2em;
    color: #fff;
    position: absolute;
    top: 15px; /* Adjusted for vertical alignment */
    right: 20px; /* Adjusted for horizontal alignment */
}

.menu-toggle {
    font-size: 1.5em;
    background: none;
    border: none;
    color: #fff;
    cursor: pointer;
    display: none; /* Hidden on desktop */
}

/* Navigation */
nav {
    background-color: #333;
    width: 100%;
}

nav ul {
    list-style-type: none;
    display: flex;
    justify-content: center;
    padding: 0;
    margin: 0;
}

nav ul li {
    margin: 0 20px;
}

nav ul li a {
    color: #fff;
    text-decoration: none;
    font-weight: bold;
    padding: 10px 15px;
    display: block;
}

nav ul li a:hover {
    background-color: #4CAF50;
    color: #fff;
    border-radius: 5px;
}

/* Responsive Menu */
@media (max-width: 768px) {
    .menu-toggle {
        display: block;
    }

    nav ul {
        display: none;
        flex-direction: column;
        text-align: center;
        background-color: #333;
    }

    nav ul.active {
        display: flex;
    }
}
